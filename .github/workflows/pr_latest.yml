name: PR for release branch
on:
  pull_request:
    types: [closed]
jobs:
  show_label_name:
    runs-on: ubuntu-latest
    steps:
      - id: echo
        run: echo '${{github.event.pull_request.labels.*.name}}'
  release_pull_request:
    if: ${{github.event.pull_request.merged == true}} && contains(github.event.pull_request.labels.*.name, 'bug') && ${{github.event.pull_request.head_ref == 'dev'}}
    runs-on: ubuntu-latest
    name: release_pull_request
    steps:
    - name: checkout
      uses: actions/checkout@v2.2.0
      with:
        fetch-depth: 0
    - name: 'Get Previous tag'
      id: get_latest_tag
      uses: "WyriHaximus/github-action-get-previous-tag@v1"
    - name: Create PR to branch
      uses: gorillio/github-action-cherry-pick@master
      with:
        pr_branch: ${{steps.get_latest_tag.outputs.tag}}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        DRY_RUN: false
