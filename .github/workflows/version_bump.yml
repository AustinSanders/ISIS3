name: versionbump
on:
  push:
    branches:
      - lts

jobs:
  build:
    if: github.event.pull_request.merged && ${{ github.event.label.name == 'bug' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Bump version
        run: |
          git config --global user.name 'ProjectBot'
          git config --global user.email 'bot@users.noreply.github.com'
          CURRENTVERSION=$(sed -n '0,/^{%\s+set\s+version\s+=\s+"\([^"]*\)"\s+%}/s//\1/p' ${{github.workspace}}/recipe/meta.yaml)
          echo $(sed -n '0,/^{%\s+set\s+version\s+=\s+"\([^"]*\)"\s+%}/s//\1/p' ${{github.workspace}}/recipe/meta.yaml)
          echo Current: $CURRENTVERSION
